# PV800 Demo (Angular)

这是一个PV800运行环境示例工程。

## 功能说明

- 每个控件对应一个组件
- 通过 `@Input` 配置颜色、尺寸和布局

## 运行项目

```bash
npm install
npm serve
```

启动后访问 `http://localhost:4200/`。

## 构建

```bash
npm run build
```

构建产物位于 `dist/pv800-demo/`。
